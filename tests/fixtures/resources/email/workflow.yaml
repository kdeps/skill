apiVersion: kdeps.io/v1
kind: Workflow

metadata:
  name: skill-test-email
  version: "1.0.0"
  description: Skill fixture — email resource (schema validation only)
  targetActionId: respond

settings:
  apiServer:
    portNum: 17611
    routes:
      - path: /api/v1/email
        methods: [POST]
  agentSettings:
    timezone: UTC