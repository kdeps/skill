apiVersion: kdeps.io/v1
kind: Workflow

metadata:
  name: skill-test-chat
  version: "1.0.0"
  description: Skill fixture — chat resource
  targetActionId: respond

settings:
  apiServer:
    portNum: 17609
    routes:
      - path: /api/v1/chat
        methods: [POST]
  agentSettings:
    timezone: UTC
    installOllama: true