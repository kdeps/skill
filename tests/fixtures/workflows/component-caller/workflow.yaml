apiVersion: kdeps.io/v1
kind: Workflow

metadata:
  name: skill-component-caller
  version: "1.0.0"
  description: Parent workflow that calls a local component
  targetActionId: respond

settings:
  apiServer:
    portNum: 17616
    routes:
      - path: /api/v1/transform
        methods: [POST]
  agentSettings:
    timezone: UTC
    pythonVersion: "3.12"